# Astryx Flutter showcase

Cross-platform reference application for `astryx_flutter`.

```sh
flutter pub get
flutter run -d chrome
```

The app assembles the package's five Atomic Design layers, shows both neutral
themes, and visualizes the default REST, GraphQL, SSE, and HATEOAS registry.
Example endpoints are documentation-only; the showcase makes no external
requests.
